# Database connection URL
# Format: postgres://user:password@host:port/database?sslmode=disable
DATABASE_URL=postgres://alebus:alebus@localhost:5432/alebus?sslmode=disable

# Postgres credentials (used by docker-compose)
POSTGRES_DB=alebus
POSTGRES_USER=alebus
POSTGRES_PASSWORD=alebus
